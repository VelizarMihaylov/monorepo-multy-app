declare type SpinnerProps = {
    border: string;
    topBorder: string;
    width: number;
    height: number;
};
export declare const spinner: ({ border, topBorder, width, height }: SpinnerProps) => string;
export {};
